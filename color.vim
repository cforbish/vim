
"***************************************************
" Set colors for vim:
"***************************************************

if (has("gui_win32"))
	if (&background == "light")
		colorscheme default
	else
		" colorscheme koehler
		colorscheme elflord
	endif
	syntax  on
endif

